"""Analytics package.

Pure, deterministic market calculations operating on domain data only. No
external dependencies. Every threshold is injected from configuration so the
package has no magic numbers.
"""
