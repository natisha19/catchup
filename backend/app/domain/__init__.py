"""Domain layer.

The domain has no dependency on FastAPI, SQLAlchemy, yfinance, or any
external framework. It defines entities, value objects, enums and the
interfaces (ports) that the application layer depends on.
"""
